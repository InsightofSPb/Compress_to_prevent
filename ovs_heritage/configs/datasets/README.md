# Dataset configuration

`heritage_facades_v2.py` is the 12-class runtime adapter. Existing repository
MMSeg configs are legacy 11-class configurations (`heritage_facades_v1_11classes`)
and are intentionally not modified or silently reinterpreted.
